export const TICKER_SELECTOR = '[data-ticker], .ticker-wrapper';
export const TICKER_CLONE_CLASS = 'ticker-clone';
export const TICKER_TRACK_SELECTOR = '[data-ticker-track], .ticker-track';
export const TICKER_CONTENT_SELECTOR = '[data-ticker-content], .ticker-content';

export const REDUCED_MOTION_QUERY = '(prefers-reduced-motion: reduce)';

export const FOCUSABLE_SELECTOR =
  'a[href], area[href], button, input, select, textarea, iframe, [tabindex], [contenteditable="true"]';

export const DEFAULT_DURATION = 20;
export const DEFAULT_DIRECTION: 'left' | 'right' = 'left';
export const DEFAULT_PAUSE_ON_HOVER = false;

export const RESIZE_DEBOUNCE_MS = 150;
export const INTERSECTION_ROOT_MARGIN = '120px 0px';